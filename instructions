## Getting Started

Requirements:
- [NODE] - v14 +


First, Install dependencies in every project:

```bash
  npm install
```

Then, run the development server:

```bash
# On Backend Project
. .env && npx gulp
source .env && npx gulp
# On FrontEnd Project
npm start
```

Reposiroty Urls
- [BACK] - Click me(https://github.com/EdgarSalasV/testCommitHistoryBack)
- [FRONT] - Click me(https://github.com/EdgarSalasV/testCommitHistoryFront2)
